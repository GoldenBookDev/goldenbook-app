import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  googleButtonContent: {
    flexDirection: "row",
    alignItems: "center",
  },
  googleIcon: {
    marginRight: 8,
  },
});
